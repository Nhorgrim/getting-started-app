name: my-first-workflow
on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v2

      - name: Build the Docker image
        run: |
          docker build -t theogrigner/tudo-alpine .
